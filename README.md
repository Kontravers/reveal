# Reveal

Cursor-reactive section backgrounds. A looping video sits behind the page; a
soft lens under the pointer reveals it as Bayer dither, color dither, or live
color. One WebGL pass. Drop-in per section.

Tune it in the instrument, copy the React or HTML snippet.

Video should be same-origin or CORS-enabled, muted, looping, around 480p.
